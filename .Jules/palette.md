## 2024-11-20 - Disabled Button Tooltips
**Learning:** Improve UX for conditionally disabled buttons by explaining the disabled state using `title` attributes on wrapper spans.
**Action:** Wrap disabled buttons in a `<span>` with `title` and conditionally apply `tabIndex` to preserve accessibility and layout.
